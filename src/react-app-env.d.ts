// eslint-disable-next-line
/// <reference types="react-scripts" />

interface Person {
  name: string;
  sex: 'm' | 'f';
  born: number;
  died: number;
  motherName: string;
  fatherName: string;
  slug: string;
}

interface Good {
  id: number;
  name: string;
  color: string;
}
